# Edge API base URL — change to the actual hostname/IP when deploying remotely
NEXT_PUBLIC_API_URL=http://localhost:8000

# Machine identifier shown in the UI (must match the edge appliance config)
NEXT_PUBLIC_MACHINE_ID=MACHINE-00001
